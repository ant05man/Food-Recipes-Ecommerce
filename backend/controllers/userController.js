// userController.js (After removing registration and login functionalities)
const User = require('../models/User');

const getUserProfile = async (req, res) => {
  // Implementation for getting user profile
};

module.exports = {
  getUserProfile,
};
